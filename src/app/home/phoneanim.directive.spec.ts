import { PhoneanimDirective } from './phoneanim.directive';

describe('PhoneanimDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneanimDirective();
    expect(directive).toBeTruthy();
  });
});
