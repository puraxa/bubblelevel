import { BubbleLevelDirective } from './bubble-level.directive';

describe('BubbleLevelDirective', () => {
  it('should create an instance', () => {
    const directive = new BubbleLevelDirective();
    expect(directive).toBeTruthy();
  });
});
